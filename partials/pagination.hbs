<nav class="bg-white w-full flex relative justify-center items-center px-8 h-20" role="navigation">
  {{#if prev}}
  <a class="w-1/2 flex items-center justify-center text-purple" href="{{page_url prev}}">← Newer Posts</a>
  {{/if}}
  {{!-- <span>Page {{page}} of {{pages}}</span>  --}}
  {{#if next}}
  <a class="w-1/2 flex items-center justify-center text-purple" href="{{page_url next}}">Older Posts →</a>
  {{/if}}
</nav>